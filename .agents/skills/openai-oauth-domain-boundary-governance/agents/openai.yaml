interface:
  display_name: "OpenAI OAuth Domain Boundary Governance"
  short_description: "OAuth、API key、usage、upstream 与持久化真源治理 Domain"
  default_prompt: "Use $openai-oauth-domain-boundary-governance 收敛 OAuth、key/quota、usage、upstream 与持久化配置的 owning layer 和合同。"
